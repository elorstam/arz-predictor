# ARZ Studio brand asset

Place the approved white ARZ Studio vector logo at:

`public/branding/arz-studio-white.svg`

The sidebar automatically uses it. Until the approved asset is present, the UI renders a clean typographic fallback and never shows a broken image.
